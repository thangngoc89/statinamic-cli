module.exports = {
  properties: {
    name: {
      pattern: /^[a-zA-Z0-9\-]+$/,
      message: 'Name must be only letters or dashes',
      type: string,
      required: true
    },
    url: {
      pattern: //
      type: string,
      
    }
  }
};
